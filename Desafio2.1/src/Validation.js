//recebe os dados e faz a validação e retorna alguma coisa

class Validation {
  JSONValidation(json) {
    //"quebrar" o json para validar cada dado
    //talvez já receber um array de objetos ou ter uma função só pra isso
  }
}

module.exports = Validation;